<h1>Mechanic: <%= @mechanic.name %></h1>
<p>Years of Experience: <%= @mechanic.years_of_experience %></p>
<p>Current rides they’re working on:</p>
<ul class="rides">
  <% @mechanic.alphaetical_rides.each do |ride| %>
  <li><%= ride.name %></li>
  <% end %>
</ul>

<p>Add a ride to workload:</p>
<%= form_tag "/mechanics/#{@mechanic.id}", method: :get do %>
  <%= label_tag :search, "Ride Id" %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Submit" %>
<% end %>
